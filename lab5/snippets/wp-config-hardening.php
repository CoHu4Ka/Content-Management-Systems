<?php
/**
 * ============================================================
 *  wp-config.php — Security Hardening Snippets
 *  Лабораторная работа №5 · Безопасность WordPress
 * ============================================================
 *
 *  ИНСТРУКЦИЯ:
 *  Вставьте блоки ниже в файл wp-config.php ПЕРЕД строкой:
 *  /* That's all, stop editing! Happy publishing. */
 * ============================================================
 */

// ──────────────────────────────────────────────
// 1. Режим отладки (только на локальной среде!)
// ──────────────────────────────────────────────
define( 'WP_DEBUG',         true  );   // включить отладку
define( 'WP_DEBUG_LOG',     true  );   // писать ошибки в /wp-content/debug.log
define( 'WP_DEBUG_DISPLAY', false );   // НЕ показывать ошибки на странице

// ──────────────────────────────────────────────
// 2. Запрет редактирования файлов из админки
//    Предотвращает изменение тем/плагинов через
//    Внешний вид → Редактор / Плагины → Редактор
// ──────────────────────────────────────────────
define( 'DISALLOW_FILE_EDIT', true );

// ──────────────────────────────────────────────
// 3. Запрет установки/обновления плагинов и тем
//    через интерфейс (опционально, для prod)
// ──────────────────────────────────────────────
// define( 'DISALLOW_FILE_MODS', true );  // раскомментировать на продакшене

// ──────────────────────────────────────────────
// 4. Уникальные ключи и соли аутентификации
//    Сгенерировать НОВЫЕ на:
//    https://api.wordpress.org/secret-key/1.1/salt/
// ──────────────────────────────────────────────
define( 'AUTH_KEY',         'put your unique phrase here' );
define( 'SECURE_AUTH_KEY',  'put your unique phrase here' );
define( 'LOGGED_IN_KEY',    'put your unique phrase here' );
define( 'NONCE_KEY',        'put your unique phrase here' );
define( 'AUTH_SALT',        'put your unique phrase here' );
define( 'SECURE_AUTH_SALT', 'put your unique phrase here' );
define( 'LOGGED_IN_SALT',   'put your unique phrase here' );
define( 'NONCE_SALT',       'put your unique phrase here' );

// ──────────────────────────────────────────────
// 5. Изменить префикс таблиц БД
//    (менять ДО установки или через плагин миграции)
// ──────────────────────────────────────────────
$table_prefix = 'usm52_';   // вместо стандартного wp_

// ──────────────────────────────────────────────
// 6. Принудительный HTTPS для авторизации
// ──────────────────────────────────────────────
define( 'FORCE_SSL_ADMIN', true );

// ──────────────────────────────────────────────
// 7. Ограничить количество ревизий записей
// ──────────────────────────────────────────────
define( 'WP_POST_REVISIONS', 5 );

// ──────────────────────────────────────────────
// 8. Автоматические обновления ядра WordPress
// ──────────────────────────────────────────────
define( 'WP_AUTO_UPDATE_CORE', 'minor' );  // 'true' — все, 'minor' — только патчи

// ──────────────────────────────────────────────
// 9. Отключить XML-RPC (если не нужен)
//    Альтернатива — через AIOS или .htaccess
// ──────────────────────────────────────────────
// add_filter( 'xmlrpc_enabled', '__return_false' );
// Лучше отключать через .htaccess (см. файл htaccess-hardening.conf)
